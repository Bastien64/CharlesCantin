backend:
  name: git-gateway
  branch: main
  publish_mode: editorial_workflow
media_folder: "images"

collections:
  - name: "posts"
    label: "Post"
    folder: "_posts"
    filter:
      field: language
      value: fr
    fields:
      - {label: "Language", name: "language"}
